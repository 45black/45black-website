fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.8
python-dotenv>=1.0

# Inference — Gemini / Vertex AI
google-genai>=0.3

# Persistence — optional, only needed for STORE_BACKEND=firestore
google-cloud-firestore>=2.18

# Optional — only needed if you also want to use the Claude harness.
anthropic>=0.40
