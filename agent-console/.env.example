# Harness: "claude" (Anthropic Managed Agents) or "gemini" (local + Gemini 3 Pro)
AGENT_HARNESS=gemini

# Anthropic (only used when AGENT_HARNESS=claude)
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_API_BASE=https://api.anthropic.com

# Gemini (only used when AGENT_HARNESS=gemini)
# Prefer Application Default Credentials: `gcloud auth application-default login`
# — this shares quota with the Gemini CLI free tier (AI Studio).
# Fall back to an API key if you want the paid Vertex / AI Studio tier.
GEMINI_API_KEY=

# Console
CONSOLE_HOST=127.0.0.1
CONSOLE_PORT=8787

# Google Workspace MCP (community server)
# https://github.com/taylorwilsdon/google_workspace_mcp
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_TRIAGE_LABEL=45black/triaged
